name: Run Tests

on:
  push:
    branches:
      - '*'
  pull_request:
    branches: 
      - '*'

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install uv
        uv sync

    - name: Run type checks with Mypy
      run: |
        uv run mypy src/ tests/ *.py  # Run mypy with the strict flag to check for type errors in your code

    - name: Run tests with pytest
      run: |
        uv run pytest tests/ --disable-warnings --maxfail=1  # Customize test flags as needed

